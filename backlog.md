需求：
生成rkb的导入歌单文件

直接操纵rkb的sqlite数据库

歌单选择区应该也可以全选或shift选择

加AI，预测分类

曲目右键可以加个选项“重新分析指纹并加入指纹库”（考虑下重新对比去重也加上）

根据元数据标签批量生成文件名

导出后删除曲目，歌单的角标并没有自动更新数量

正在播放的库图标也应该变蓝

flac转mp3，封面数据没过去

做自动混音录set

打开歌曲失败这样的错误也要打印进log.txt

BPM的悬浮提示会挡住用户tap的bpm

每次升级完成后都应该清理一下上个版本的报错日志

能否做到在拖拽的时候依然能滚轮滚动歌单列表

应该允许用户自建库，但是要想好添加歌曲操作怎么处理

移动歌曲至其他歌单，波形等信息会消失

筛选弹框没有兼容回车确认（在input里输入的时候）

在分析中的歌单，滚动会失效，滚动一段距离后会自己回到正在播放的歌曲

筛选曲目可以加个排除项