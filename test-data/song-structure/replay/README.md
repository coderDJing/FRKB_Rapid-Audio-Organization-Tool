# Replay 资产约定

完整音频不进 Git。若未来为了 CI 或快速调参提交 replay，只允许保存不能还原音乐的紧凑 bar
特征、网格和必要的生产输入版本信息，并且必须由生产特征提取链路生成，禁止在 benchmark 中
复制另一套近似特征算法。

本地可复用缓存统一放在被忽略的 `structure-analysis-lab/replay/`。仓库内 `replay/` 目前只保留
本说明；在格式、版权风险和可重放一致性验证完成前，不提交二进制 replay。
